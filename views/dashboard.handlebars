<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      background-color: #292929;
      color: #ffffff;
    }

    .join-container {
      max-width: 600px;
      margin: 60px auto;
    }

    .join-header {
      padding: 30px;
      font-family: 'Merienda One', cursive;
      font size: 1.5rem;
    }

    .main {
      padding: 30px;
    }

    .enter-room {
      margin-bottom: 10px;
    }

    .form-content {
      margin-bottom: 20px;
      display: flex;
      flex-direction: column;
    }

    .btns {
      display: flex;
      justify-content: flex-start;
    }

    .form-content input[type="text"] {
      font-size: 16px;
      padding: 0 1rem;
      height: 40px;
      width: 70%;
      border: none;
      border-radius: 10px;
      background-color: #ececec;
      color: black;
    }

    .form-content input[type="text"]:focus {
      outline: none;
    }

    .form-content label {
      margin-bottom: 5px;
    }

    .form-content select {
      font-size: 16px;
      padding: 0.5rem;
      height: 40px;
      width: 50%;
      border: none;
      border-radius: 10px;
      background-color: #ececec;
      color: black;
      margin-bottom: 1rem;
    }

    .colorInput {
      display: flex;
      justify-content: space-between;
      width: 10rem;
    }

    .form-content select:focus {
      outline: none;
    }

    .btns {
      display: flex;
      justify-content: space-between;
      width: 70%;
    }

    .button {
      margin-top: 20px;
      padding: 5px;
      margin-top: 20px;
      padding: 10px 15px;
      font-size: 16px;
      cursor: pointer;
      border: none;
      color: white;
      border-radius: 10px;
      background-color: rgb(38, 90, 185);
    }

    .join-header-info {
      font-style: italic;
      font-size: 1.5rem;
    }

    h3 {
      text-align: center;
    }
  </style>
  <title>Dashboard</title>

</head>

<body>
  <div class="join-container">
    <header class="join-header">
      <h1>Express Chat</h1>
      <p class="join-header-info">It's unopinionated and fast...</p>
    </header>

    {{#if loggedIn}}
    <div class="main">
      <div class="enter-room">
        <h2>Enter a room</h2>
      </div>
      <form action="/chat">
        <div class="form-content">
          <label for"username">Nickname: </label>
          <input type="text" name="nickname" id="nickname" placeholder="Enter nickname ..." required />
        </div>
        <div class="form-content">
          <label for"room">Room: </label>
          <select name="room" id=" room">
            {{!-- <option value="" disabled selected>Select your option</option> --}}
            <option value="Code">Code</option>
            <option value="Music">Music</option>
            <option value="Fun">Fun</option>
            <option value="Events">Events</option>
          </select>
          <div class="colorInput">
            <label for="color">Chat Color: </label>
            <input type="color" name="color" id="color">

          </div>

        </div>
        <button type="submit" class="button">Join Chat</button>
      </form>
      <div class="btns">
        <form action="/user/logout">
          <input type="submit" class="button" value="Log out" />
        </form>
      </div>

      <div />
    </div>
    {{else}}
    <div class="h3">
      <h3>You need to be logged in to enter a chat room</h3>
    </div>
    <div class="btns">
      <form action="/user/login">
        <input type="submit" class="button" value="Login" />
      </form>

      <form action="/user/register">
        <input type="submit" class="button" value="Register" />
      </form>
    </div>


    {{/if}}

  </div>

</body>

</html>